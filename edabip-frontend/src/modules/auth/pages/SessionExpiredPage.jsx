import SessionExpiredForm from '../components/SessionExpiredForm.jsx';
import './SessionExpiredPage.css';

function SessionExpiredPage() {
  return (
    <div className="session-expired-page">
      <SessionExpiredForm />
    </div>
  );
}

export default SessionExpiredPage;
